#!/usr/bin/env bash

# Helper to read .rela information from kernel.elf
export SCRIPTDIR="$(dirname $0)"
source "${SCRIPTDIR}"/common

objdump="${SCRIPTDIR}/cross-cc/bin/aarch64-none-elf-objdump"
objdir="$SCRIPTDIR/../obj"

$objdump $objdir/kernel/kernel.elf -D -z